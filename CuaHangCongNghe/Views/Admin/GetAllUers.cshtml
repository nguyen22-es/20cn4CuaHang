@model List<CuaHangCongNghe.Models.OrderViewModel>

<div class="row">
    <div class="col-md-3 my-4">
        <div class="list-group ">
            <a asp-action="GetOrders" class="list-group-item list-group-item-action">Đơn đặt hàng </a>
            <a asp-action="GetUsers" class="list-group-item list-group-item-action">người dùng</a>
            <a asp-action="GetRoles" class="list-group-item list-group-item-action">vai trò</a>
            <a asp-action="AddProduct" class="list-group-item list-group-item-action">thêm sản phẩm</a>
        </div>
    </div>
    <div class="col-md-9 my-4">
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-12">
                        <h4>danh sách đơn hàng</h4>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12 table-responsive">
                        <table class="table table-hover table-sm">
                            <thead>
                                <tr>
                                    <th>№</th>
                                    <th>Сумма</th>
                                    <th>Статус</th>
                                    <th>Пользователь (логин)</th>
                                    <th>Дата</th>
                                    <th>Время</th>
                                </tr>
                            </thead>
                            <tbody>
                                @for (int i = Model.Count - 1; i >= 0; i--)
                                {
                                    var order = Model[i];
                                    <tr class="table-row" data-href="/Admin/GetOrder/@order.Id">
                                        <td>@order.</td>
                                        <td>@order.FullPrice &#8381;</td>
                                        <td>@order.Status</td>
                                        <td></td>
                                        <td></td>
                                        <td></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>